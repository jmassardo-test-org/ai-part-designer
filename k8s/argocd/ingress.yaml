---
# ArgoCD Ingress Configuration
# Provides access to the ArgoCD UI
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: argocd-server-ingress
  namespace: argocd
  labels:
    app.kubernetes.io/name: argocd-server
    app.kubernetes.io/component: server
  annotations:
    # Use HTTP backend for local dev (ArgoCD runs HTTP on 8080)
    nginx.ingress.kubernetes.io/backend-protocol: "HTTP"
    nginx.ingress.kubernetes.io/ssl-redirect: "false"

    # Security headers
    nginx.ingress.kubernetes.io/configuration-snippet: |
      more_set_headers "X-Frame-Options: DENY";
      more_set_headers "X-Content-Type-Options: nosniff";
      more_set_headers "X-XSS-Protection: 1; mode=block";

    # Rate limiting
    nginx.ingress.kubernetes.io/limit-rps: "10"

spec:
  ingressClassName: nginx

  rules:
    - host: argocd.assemblematic.ai
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: argocd-server
                port:
                  name: http

---
# Alternative: ArgoCD Server Service with LoadBalancer
# Use this if you prefer LoadBalancer instead of Ingress
# Uncomment and modify as needed
# apiVersion: v1
# kind: Service
# metadata:
#   name: argocd-server-lb
#   namespace: argocd
#   labels:
#     app.kubernetes.io/name: argocd-server
# spec:
#   type: LoadBalancer
#   ports:
#     - name: http
#       port: 80
#       targetPort: 8080
#       protocol: TCP
#     - name: https
#       port: 443
#       targetPort: 8080
#       protocol: TCP
#   selector:
#     app.kubernetes.io/name: argocd-server
